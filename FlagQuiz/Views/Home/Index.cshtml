@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>Welcome to FlagQuiz !</h1>
    <p class="lead">Test yourself in this Quiz about Flags. Good Luck!</p>
    <button onclick="location.href='@(Url.Action("Flags", "FlagQuiz", new { indexQuestion = 1 }))'" class="btn btn-primary btn-lg">Start</button>
</div>
